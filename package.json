{
  "name": "dynara-monorepo",
  "type": "module",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --conditions development --watch packages/dev-app/src/dev-app.ts",
    "build": "bun --filter 'dynara' build",
    "test": "bun --filter 'dynara' test",
    "release": "bun --filter 'dynara' release"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
