{
  "name": "marci-monorepo",
  "type": "module",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --conditions development --watch packages/dev-app/src/dev-app.ts",
    "build": "bun --filter '@den59k/marci' build",
    "release": "bun --filter '@den59k/marci' release"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
